def check_for_dupes(num_list):
	current_n = num_list(0)
	for i in range(1, (len(num_list) - 1))
next_n = num_list(i)
if next_n > current_n:
	greatest_n = next_n
else:
	greatest_n = current_n
return greatest_n
